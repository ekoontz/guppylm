# Introduction

This is my plan to learn about LLMs using this repo (guppylm).

# Steps

1. Get guppylm working in your environment
1.1. Dependencies: what are they?
1.1.1. torch
1.1.2. tokenizers
1.1.3. tqdm
1.1.4. numpy
1.1.5. datasets
2. How does `python -m guppylm chat` work at a high-level?
3. Runtime
  3.1. What happens at startup leading up to the "You>" prompt?
  3.2. What happens when you talk to GuppyLM (i.e. when you type something and hit the "return" key)?
4. Models
  4.1. Files

# Files

The `checkpoints/best_model.pt` (downloaded via `python -m guppylm download`), is a zip file.

  4.2. Internal Structure
  4.3. Creating models from training data
5. Hardware and OS specificities
  4.1. MacOS
  4.2. Linux

